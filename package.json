{
  "name": "forge-deploy-proxy",
  "version": "0.0.4",
  "description": "proxy deployment for forge-deploy",
  "license": "MIT",
  "files": [
    "src",
    "templates"
  ],
  "peerDependencies": {
    "forge-deploy": ">=0.0.37 && < 0.1.0"
  }
}
