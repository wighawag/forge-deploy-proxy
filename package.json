{
  "name": "forge-deploy-proxy",
  "version": "0.0.5",
  "description": "proxy deployment for forge-deploy",
  "license": "MIT",
  "files": [
    "src",
    "templates"
  ],
  "peerDependencies": {
    "forge-deploy": ">=0.1.0 && < 0.2.0"
  }
}
